name: Test Workflow

on:
  push:
    branches: [ feat/automation ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Test workflow
      run: echo "Workflow is running!"