version: '3.8'

services:
  pyguizer:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    command: python -m pyguizer --host 0.0.0.0 --port 8000
